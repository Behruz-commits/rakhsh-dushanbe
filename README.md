# raкhsh — Умные светофоры Душанбе

## Цель проекта
Создать адаптивную цифровую систему управления городским движением в Душанбе с возможностью масштабирования на всю транспортную сеть.  
Пилотная версия демонстрирует работу **ЦУАК** и локальных контроллеров через **симуляцию потоков машин и пешеходов**, а также визуализацию состояния светофоров.

---

## Архитектура

### 1. Локальные контроллеры (`controllers/`)
- `traffic_light.py` — управление состояниями светофора (RED → GREEN → YELLOW → RED).  
- `sensor_interface.py` — симуляция потока транспорта и пешеходов.  
- `network_comm.py` — отправка данных на центральный узел (ЦУАК).

### 2. Центральный узел (`central_node/`)
- `main.py` — сбор данных с контроллеров и симулятора, запуск оптимизации.  
- `data_collector.py` — сбор информации о движении.  
- `ai_optimizer.py` — принятие решений: увеличение зелёного времени для машин или пешеходов.  
- `db_interface.py` — запись логов работы системы.

### 3. Симулятор перекрёстков (`simulator/`)
- `simulate_intersection.py` — интеграция контроллеров, симуляция перекрёстка и взаимодействие с ЦУАК.  
- `visualization.py` — текстовая визуализация состояния светофоров (🔴🟡🟢).

### 4. Конфигурации (`config/`)
- `intersections.yaml` — список перекрёстков.  
- `network_config.json` — параметры связи с ЦУАК.  
- `ai_settings.json` — пороги ИИ для изменения цикла светофоров.

### 5. Документация (`docs/`)
- Архитектура, развертывание, разработка, дорожная карта проекта.

---

## Работа демоверсии
- Все модули запускаются в **отдельных окнах CMD** через `run_pilot.bat`.  
- Локальные контроллеры симулируют циклы светофоров.  
- Симуляция потоков транспорта и пешеходов генерируется случайно.  
- ЦУАК **обрабатывает данные** и принимает решения по регулировке светофоров.  
- Визуализация демонстрирует состояния перекрёстков.

---

## Запуск пилота
1. Убедитесь, что установлен Python 3.11+.  
2. Установите зависимости:
```cmd
pip install opencv-python numpy
